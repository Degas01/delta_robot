// ikin_server.cpp
// Exercise 5: the /ikin service callback calls inverse_kinematics() on the
// requested position, then sendToSerial() forwards the joint angles to the
// Arduino. Requests that resolve to NaN (unreachable positions) are ignored.
//
// TODO: paste in your actual ikin_server implementation here.
